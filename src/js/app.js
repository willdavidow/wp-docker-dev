/*
Want to import a local file? Just add it in
the js directory and make sure you're loading it like so...
*/
// import whatever from './local/file.js';
((window) => {

    console.log('app.js loaded');

})(window);